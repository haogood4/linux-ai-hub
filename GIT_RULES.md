# Git 操作规则（GIT_RULES）

> 适用范围：本仓库所有 Git 操作（agent 与协作者一致）。
> AGENTS.md 中引用的「Git 操作规则」以此文件为准。

## 1. 基本约定

- 用户说「提交」时，默认只创建本地 commit，**不执行 push**。
- 未经明确授权，不得 push、合并、部署、强制推送（`--force`）或重写历史（rebase / amend 历史提交）。
- 提交前必须检查：`git diff`、敏感信息、测试与构建结果。
- 提交后必须报告：提交哈希、检查结果、剩余未提交修改、是否已 push。
- 不使用 `git -i` 交互式操作；不跳过 hooks；不创建空提交。

## 2. 禁止提交的内容

- `.env`、密码、Token、私钥、用户数据或服务器凭据。
- Linux ISO 镜像、发行版 Logo 图片、用户故事（无授权不得入库）。
- 与本项目无关的第三方二进制或大文件（超过约 1MB 需评估）。

## 3. 改动前检查

- 不得覆盖、删除或提交用户已有的无关修改。
- 涉及以下内容的改动必须同步配套：
  - **中英文内容**：修改一种语言必须同步另一种（当前仅中文，i18n 后置）。
  - **算法 / 埋点 / 云端 / 部署**：必须同步测试、文档与隐私说明。
  - **安装 / 分区 / 双系统页面**：必须保留备份风险提示（AGENTS.md §7）。
- 重大技术决策记录到 `docs/DECISIONS.md`（ADR）。

## 4. 提交信息规范

- 格式：`<type>: <摘要>`，type 参考 conventional commits（feat / fix / docs / chore / refactor / test）。
- 摘要使用中文，一行描述核心变更；必要时附正文说明原因。
- 一个提交只做一件事，避免混合无关改动。

## 5. 推送与远端

- 远端：`origin`（GitHub 私有仓库 haogood4/linux-ai-hub）。
- 推送前确认远端为私有可见性（不可见）。
- 推送失败时先检查远端状态，不要强制推送掩盖问题。